SELECT
     ISNULL(e.FORMER_ENTRY_CODE, e.CODE)           AS TRCD_CODE_BK
    , UPPER(e.DESCRIPTION)     AS TRCD_CODE_DESCRIPTION,
          CAST(CASE WHEN e.IS_ACTIVE   = 'Y' THEN 1 ELSE 0 END AS BIT) as TRCD_IS_ACTIVE
FROM SICS.ENTRY_CODE AS e
ORDER BY
      e.CODE;
