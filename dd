SELECT
    ISO_ALPHA   AS curr_bk,
    UPPER(ISO_NAME_ENGLISH) AS curr_name,
       CAST(CASE WHEN IS_ACTIVE   = 'Y' THEN 1 ELSE 0 END AS BIT) as TRCD_IS_ACTIVE
FROM SICS.CNU_CURRENCY
ORDER BY ISO_ALPHA;
