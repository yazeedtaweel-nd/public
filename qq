SELECT
    rd.SUBCLASS,
    rd.CODE,
    CASE rd.CODE
        WHEN 'QA'      THEN 'PRO RATA - QS'
        WHEN 'SURPLUS' THEN 'PRO RATA - SURPLUS'
        ELSE rd.NAME
    END AS NAME,
    rd.EXTERNAL_CODE,
    rd.IS_ACTIVE
FROM SICS.REFERENCE_DATA AS rd
WHERE rd.SUBCLASS = 22
ORDER BY rd.CODE;
