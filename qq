SELECT
      rd.CODE,
      rd.NAME,
      rd.EXTERNAL_CODE,
      rd.SUBCLASS,
      rd.IS_ACTIVE,
      rd.IS_DEFAULT,
      rd.IS_STATIC,
      rd.IS_USER_DEFINED
FROM SICS.REFERENCE_DATA AS rd
WHERE rd.SUBCLASS = 95
ORDER BY rd.CODE;
