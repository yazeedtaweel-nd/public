SELECT
    rd.CODE              AS DCLR_BK,
    rd.NAME              AS DCLR_NAME,
    rd.EXTERNAL_CODE,
    rd.SUBCLASS,
    rd.IS_ACTIVE,
    rd.IS_DEFAULT,
    rd.IS_STATIC,
    rd.IS_USER_DEFINED
FROM dbo.REFERENCE_DATA AS rd
WHERE rd.SUBCLASS = 106
ORDER BY rd.CODE;
