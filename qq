SELECT
    rd.CODE              AS PRMQ_BK,
    rd.NAME              AS PRMQ_NAME,
    rd.IS_ACTIVE,
    ROW_NUMBER() OVER (ORDER BY rd.NAME, rd.CODE) AS PRMQ_SEQ
FROM SICS.REFERENCE_DATA rd
WHERE rd.SUBCLASS = 134
ORDER BY
    PRMQ_SEQ;
