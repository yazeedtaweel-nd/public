SELECT TOP (100)
    t.name AS table_name,
    c.name AS column_name
FROM sys.tables t
JOIN sys.columns c ON c.object_id = t.object_id
WHERE c.name LIKE '%DEP%'
   OR t.name LIKE '%REF_DATA%DEP%'
   OR t.name LIKE '%DEP%REF%'
ORDER BY t.name, c.name;
