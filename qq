SELECT
    COALESCE(rd.external_code,rd.CODE)              AS LSTP_BK,
    UPPER(rd.NAME)              AS LSTP_NAME,
    CAST(CASE WHEN rd.IS_ACTIVE       = 'Y' THEN 1 ELSE 0 END AS BIT) AS LSTP_IS_ACTIVE    
FROM SICS.REFERENCE_DATA AS rd
WHERE rd.SUBCLASS = 523
ORDER BY rd.CODE;
