SELECT
    rz.CODE AS CRZ_BK,
    ISNULL(la.NAME, '') + ' - ' + rz.NAME AS CRZ_NAME,
    ISNULL(la.ISO_ALPHA_2, la.CODE) AS CTRY_COUNTRY_CODE
FROM SICS.RISK_ZONE rz
LEFT JOIN LEGAL_AREA la
    ON la.OBJECT_ID = rz.FK_COUNTRY
   AND la.SUBCLASS = 1
ORDER BY rz.CODE;
